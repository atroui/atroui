/**
 * Site chrome — two doors. Catalog, registry, Host APIs, and updates
 * live in docs / landing body (gradual revelation).
 */
export const primaryNav = [
  { href: "/docs", label: "Docs" },
  { href: "/blog", label: "Blog" },
] as const
