/** Re-export registry brand so installed blocks resolve `@/lib/brand` in docs previews. */
export {
  DEFAULT_BRAND,
  getBrand,
  getBrandMailto,
  type Brand,
} from "../registry/default/lib/brand"
