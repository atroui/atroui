/**
 * npm convenience alias. Install surface for docs/CLI is `@atroui/home-hero`
 * (registry `HomeHero` under `components/blocks/home-hero`) — keep those in sync
 * when changing hero CONTENT or layout.
 */
export { HeroAiValueProposition as HomeHero } from "../hero-ai-value-proposition"
