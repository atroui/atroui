---
"atroui": minor
---

Foundations: brand isolation via `getBrand()` / `NEXT_PUBLIC_SITE_*`, packaging cleanup, Vitest, CI, and Changesets release pipeline. SEO brand constants are call-time only (no module-load freeze).
