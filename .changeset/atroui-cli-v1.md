---
"@atroui/cli": minor
---

Add `@atroui/cli`: first-party registry installer (`add`, `list`) so users can copy AtroUI source into their repo without the shadcn CLI. Same `www.atroui.com/r` JSON as before.
